package com.mikront.util.data;

/**
 * Abstract class to extend to communicate between different places in the program and keep the data synchronized
 * using {@link LiveData}
 */
public abstract class Communicator {
    public Communicator() {}
}